# This is here for error suppression; do not mind the duplicate key error
add_com_topbar_element = {
	if = {
		limit = { always = no }
		set_variable = $element_name$
		remove_variable = $element_name$
	}
}